#include<iostream>
using namespace std;
#define pi 3.14 
int main(){
	cout<<pi;
	return 0;
}

//Macros are simply nothing but we are using constant values throughout the program 
//simply we are defining constant with name and value 
//we know that once we compile the code then our program (code) will converts into binary format like 0's and 1's but 
  //->macros are converted into binary format once we saved the code
  
  
  
  
  /*Macros are a piece of code in a program that is given some name. Whenever the compiler encounters this name,
   the compiler replaces the name with the actual piece of code. The ‘#define’ directive is used to define a macro.*
